export interface FAQItem {
  id: string;
  question: string;
  answer: string;
}

export interface IdentificationItem {
  id: string;
  text: string;
}

export interface ProcessStep {
  number: string;
  title: string;
  description: string;
}
